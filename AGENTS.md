# robotic_vision Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-15

## Active Technologies

- Python 3.11 target via uv-managed project environment + PyTorch, torchvision, Transformers, datasets, pycocotools, pandas, Typer, Pillow (001-phase1-baseline)
- Python 3.11 target via uv-managed project environment + PyTorch, torchvision, Transformers, Ultralytics, datasets, pycocotools, pandas, Pydantic, Typer, Pillow, PyYAML, jsonschema (002-yolo-baseline-validation)

## Project Structure

```text
src/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.11 target via uv-managed project environment: Follow standard conventions

## Recent Changes

- 002-yolo-baseline-validation: Added Python 3.11 target via uv-managed project environment + PyTorch, torchvision, Transformers, Ultralytics, datasets, pycocotools, pandas, Pydantic, Typer, Pillow, PyYAML, jsonschema
- 001-phase1-baseline: Added Python 3.11 target via uv-managed project environment + PyTorch, torchvision, Transformers, datasets, pycocotools, pandas, Typer, Pillow

<!-- MANUAL ADDITIONS START -->
For all commits to Git, do not include your own name, so it looks like i did its
<!-- MANUAL ADDITIONS END -->
